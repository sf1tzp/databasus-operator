---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: databasebackups.databasus.databasus.io
spec:
  group: databasus.databasus.io
  names:
    kind: DatabaseBackup
    listKind: DatabaseBackupList
    plural: databasebackups
    singular: databasebackup
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.database.type
      name: DB Type
      type: string
    - jsonPath: .status.healthStatus
      name: Health
      type: string
    - jsonPath: .status.conditions[?(@.type=="Ready")].status
      name: Ready
      type: string
    - jsonPath: .status.lastBackupTime
      name: Last Backup
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DatabaseBackup is the Schema for the databasebackups API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DatabaseBackupSpec defines the desired state of a managed
              database backup.
            properties:
              backup:
                properties:
                  encryption:
                    allOf:
                    - enum:
                      - NONE
                      - ENCRYPTED
                    - enum:
                      - NONE
                      - ENCRYPTED
                    default: NONE
                    type: string
                  interval:
                    properties:
                      cronExpression:
                        description: 5-field cron expression (required for CRON).
                        type: string
                      dayOfMonth:
                        description: Day of month 1-31 (required for MONTHLY).
                        type: integer
                      timeOfDay:
                        description: Time in "HH:MM" format (required for DAILY, WEEKLY,
                          MONTHLY).
                        type: string
                      type:
                        enum:
                        - HOURLY
                        - DAILY
                        - WEEKLY
                        - MONTHLY
                        - CRON
                        type: string
                      weekday:
                        description: Day of week 0-6, Sunday=0 (required for WEEKLY).
                        type: integer
                    required:
                    - type
                    type: object
                  isEnabled:
                    default: true
                    type: boolean
                  isRetryIfFailed:
                    default: false
                    type: boolean
                  maxFailedTriesCount:
                    type: integer
                  retentionPolicy:
                    properties:
                      count:
                        description: Number of backups to keep (for COUNT).
                        type: integer
                      gfsDays:
                        type: integer
                      gfsHours:
                        description: GFS retention fields.
                        type: integer
                      gfsMonths:
                        type: integer
                      gfsWeeks:
                        type: integer
                      gfsYears:
                        type: integer
                      timePeriod:
                        description: Duration string e.g. "7d", "30d", "3m", "1y"
                          (for TIME_PERIOD).
                        type: string
                      type:
                        enum:
                        - TIME_PERIOD
                        - COUNT
                        - GFS
                        type: string
                    required:
                    - type
                    type: object
                  sendNotificationsOn:
                    items:
                      enum:
                      - BACKUP_FAILED
                      - BACKUP_SUCCESS
                      type: string
                    type: array
                  storageRef:
                    description: Name of the Storage CRD in the same namespace.
                    type: string
                required:
                - interval
                - isEnabled
                - retentionPolicy
                - storageRef
                type: object
              database:
                properties:
                  mariadb:
                    properties:
                      database:
                        type: string
                      host:
                        type: string
                      isHttps:
                        default: false
                        type: boolean
                      passwordSecretRef:
                        description: SecretKeyRef references a key within a Kubernetes
                          Secret.
                        properties:
                          key:
                            description: Key within the Secret data.
                            type: string
                          name:
                            description: Name of the Secret in the same namespace.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        type: integer
                      username:
                        type: string
                      version:
                        type: string
                    required:
                    - host
                    - passwordSecretRef
                    - port
                    - username
                    - version
                    type: object
                  mongodb:
                    properties:
                      authDatabase:
                        type: string
                      cpuCount:
                        default: 1
                        type: integer
                      database:
                        type: string
                      host:
                        type: string
                      isDirectConnection:
                        default: false
                        type: boolean
                      isHttps:
                        default: false
                        type: boolean
                      isSrv:
                        default: false
                        type: boolean
                      passwordSecretRef:
                        description: SecretKeyRef references a key within a Kubernetes
                          Secret.
                        properties:
                          key:
                            description: Key within the Secret data.
                            type: string
                          name:
                            description: Name of the Secret in the same namespace.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        type: integer
                      username:
                        type: string
                      version:
                        type: string
                    required:
                    - database
                    - host
                    - passwordSecretRef
                    - username
                    - version
                    type: object
                  mysql:
                    properties:
                      database:
                        type: string
                      host:
                        type: string
                      isHttps:
                        default: false
                        type: boolean
                      passwordSecretRef:
                        description: SecretKeyRef references a key within a Kubernetes
                          Secret.
                        properties:
                          key:
                            description: Key within the Secret data.
                            type: string
                          name:
                            description: Name of the Secret in the same namespace.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        type: integer
                      username:
                        type: string
                      version:
                        type: string
                    required:
                    - host
                    - passwordSecretRef
                    - port
                    - username
                    - version
                    type: object
                  name:
                    type: string
                  notifierRefs:
                    description: Names of Notifier CRDs in the same namespace to attach
                      to this database.
                    items:
                      type: string
                    type: array
                  postgresql:
                    properties:
                      backupType:
                        default: PG_DUMP
                        enum:
                        - PG_DUMP
                        - WAL_V1
                        type: string
                      cpuCount:
                        default: 1
                        type: integer
                      database:
                        type: string
                      host:
                        type: string
                      includeSchemas:
                        items:
                          type: string
                        type: array
                      isHttps:
                        default: false
                        type: boolean
                      passwordSecretRef:
                        description: SecretKeyRef references a key within a Kubernetes
                          Secret.
                        properties:
                          key:
                            description: Key within the Secret data.
                            type: string
                          name:
                            description: Name of the Secret in the same namespace.
                            type: string
                        required:
                        - key
                        - name
                        type: object
                      port:
                        type: integer
                      username:
                        type: string
                      version:
                        type: string
                    required:
                    - host
                    - passwordSecretRef
                    - port
                    - username
                    - version
                    type: object
                  type:
                    enum:
                    - POSTGRES
                    - MYSQL
                    - MARIADB
                    - MONGODB
                    type: string
                required:
                - name
                - type
                type: object
              healthcheck:
                properties:
                  attemptsBeforeConsideredAsDown:
                    minimum: 1
                    type: integer
                  intervalMinutes:
                    minimum: 1
                    type: integer
                  isEnabled:
                    default: true
                    type: boolean
                  isSentNotificationWhenUnavailable:
                    default: false
                    type: boolean
                  storeAttemptsDays:
                    minimum: 1
                    type: integer
                required:
                - attemptsBeforeConsideredAsDown
                - intervalMinutes
                - isEnabled
                - storeAttemptsDays
                type: object
            required:
            - backup
            - database
            type: object
          status:
            properties:
              backupConfigId:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              databaseId:
                description: IDs assigned by the databasus API.
                type: string
              healthStatus:
                enum:
                - AVAILABLE
                - UNAVAILABLE
                - UNKNOWN
                - ""
                type: string
              lastBackupErrorMessage:
                type: string
              lastBackupTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
