## Test-environment CRs (luxor): gitea database → rustfs S3 storage.
## credentials_secret.yaml is a template — the real secret is created
## out-of-band; notifier_discord.yaml needs a discord-webhook secret.
resources:
- storage_s3.yaml
- databasebackup_gitea.yaml
# +kubebuilder:scaffold:manifestskustomizesamples
