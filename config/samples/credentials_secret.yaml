apiVersion: v1
kind: Secret
metadata:
  name: databasus-operator-credentials
  namespace: databasus
type: Opaque
stringData:
  email: admin@example.com
  password: changeme
  # workspaceName is optional - if omitted, uses the first workspace.
  # workspaceName: "My Workspace"
  # workspaceId is optional - takes precedence over workspaceName.
  # workspaceId: "xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"
